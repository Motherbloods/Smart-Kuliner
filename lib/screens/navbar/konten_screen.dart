import 'package:flutter/material.dart';

class KontenScreen extends StatelessWidget {
  const KontenScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Halaman Konten', style: TextStyle(fontSize: 24)),
    );
  }
}
